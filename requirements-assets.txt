# Только для перерисовки ассетов: tools/render_env.py (ADR-0011),
# tools/render_audio.py (ADR-0012) и tools/render_icon.py (ADR-0013).
# В CI не ставится и на запуск игры не влияет — готовые PNG, WAV, OGG и ICO
# лежат в репозитории.
#   .venv/Scripts/pip install -r requirements-assets.txt   # Windows
Pillow==12.1.0
numpy==2.4.1
soundfile==0.14.0
