# Статус разработки

Обновляется в каждом коммите, который трогает `src/`, `tests/` или `project.godot`.
Это проверяет хук `status-updated`.

| | |
|---|---|
| **Обновлено** | 2026-09-24 |
| **Текущая веха** | M22b — меню |
| **Ветка** | `feat/m22b-menu` поверх `fix/sweep-debt` и `docs/sweep` |
| **Состояние** | Веха готова: авторевью прошло, `check.ps1` зелёный. **Осталось: PR** — после PR `docs/sweep` и `fix/sweep-debt` |
| **Точка отката** | `main` держит 3D-сборку с M19; 2D осталась в истории, `d5774df` |

## Где мы сейчас

Пройдены **M0–M6**: фундамент, движение, лифты и эскалаторы, двери и документы,
бой и лампы, здание генерацией, игровой цикл, свет и атмосфера. Всё это в `main`.

Играется партия: Otto спускается с крыши тридцатиэтажного здания, собирает
документы из красных дверей — от пяти до десяти, — отбивается от агентов, уходит
в выход и попадает в следующее здание, где агенты злее. Провозился — включается
сирена. Зона этажа светла, пока над ней горит лампа; сбитая гасит её насовсем.

Пройдена **M7 целиком**: игра собрана из ассетов. Окружение рисует генератор на
Python, актёры и машина сняты с лоу-поли модели в Blender; у всего есть нормаль и блик,
и свет из M6 ложится на рельеф. Цветных коробок в кадре не осталось.

Пройдена **M8a**: игра звучит — шаг, выстрел, лифт, двери, лампы, смерти, тема
здания и мотив тревоги.

Пройдена **M8b**: игра запускается из меню и возвращается в него. Меню, HUD,
настройки, рекорды и два языка на месте.

Пройдена **M9**: сборки под Windows и Linux, версии, теги и workflow релиза.
Тег ещё не ставился.

Пройдена **M10**: крыша отдельным уровнем, ступенчатый силуэт, выпуск агентов
рядом с игроком.

Пройдена **M11**: бой сбалансирован. Оси роста сложности как в оригинале, агенты
уклоняются от пуль, живых в здании не больше восьми, числа боя — в правилах здания.
Бот проходит здание с агентами на трёх жизнях, и это проверяется тестом.

Пройдена **M12**: визуал по версии для ZX Spectrum — палитра раунда, видимая
шахта, машинное отделение на крыше и спуск по тросу.

Пройдена **M13**: нативный FullHD и качественные текстуры — мир втрое крупнее,
ассеты перерисованы с деталью, пропорции переразмечены, предел шахты виден в кадре.

M12 и M13 влиты в `main` (PR #24).

Пройдена **M14** — последняя веха на 2D: дверь агента открывается по-настоящему.
Створка ходит отдельным классом, телеграф 0.7 с, в проёме агент не мишень. Влита
в `main` (PR #25).

Пройдена **M15** — первая веха пивота: слой узлов переехал на 3D, здание из серых
коробок, бот проходит тридцать этажей с агентами. Влита в `main` (PR #26);
2D-сборка осталась в истории — слияние M14, `d5774df`.

Пройдена **M16** — актёры на скелете: риг из Blender, анимация кодом, обводка
вместо свечения. Влита в `main` (PR #27).

Пройдена **M17** — свет: наклон камеры, зоны ламп, воздух и рёбра, огоньки
читаемости. Новая механика — темнота решает, видят ли агенты Otto. Влита
в `main` (PR #28).

Пройдена **M18a** — раскладка здания: мелкая сетка мест, силуэт порогом,
пересекающиеся шахты, эскалаторы полосой и внутренние стены. Путь по зданию ищет
бот, а не гарантирует генератор. Влита в `main` (PR #30).

Пройдена **M18b** — вид и ход: эскалатор конструкцией, свет в шахте, двухэтажная
кабина и агенты, которые впервые ездят в кабинах. Влита в `main` (PR #31).

Пройдена **M18c** — пропорции по оригиналу: всё, что стоит на полу, в долях
этажа 1983 года, размеры одной таблицей, номера этажей. Влита в `main` (PR #32).

Пройдена **M18d** — бой по правилам ROM: нашёлся дизассемблер аркады, и
сложность, выпуск, выстрел, увёртка и скорости взяты из его кода. Влита
в `main` (PR #33).

Пройдена **M18e** — здание по карте оригинала: двери, красные двери и тёмные
этажи по таблицам ROM, соль партии. Влита в `main` (PR #35).

Пройдена **M19** — наполнение и задний план: ночной город в своём
перспективном виде, погода раунда, обстановка этажей, крыша по оригиналу.
Влита в `main` (PR #36).

Пройдена **M20** — детализация сцены: лифт, крыша, этажи, город, кровь и искры.
Влита в `main` (PR #37).

Пройдена **M21** — люди и машины из паков Quaternius, пуля-трассер. Влита в
`main` (PR #38).

Пройдена **M21b** — мир моделями паков: отель или офис с неоновой вывеской,
этажи с мебелью и фактурами, стальная шахта с табло кабины, крыша с техникой.
Влита в `main` (PR #39).

Пройдена **M22** — грейдинг и полировка, последняя веха пивота: уровень «Ультра»
и выбор уровня по замеру, нуар-тон, HUD неон-нуар, режимы окна до 4K, город и
погода детальнее. Влита в `main` (PR #40). Идёт сверка всей документации.

## Что работает

- `OttoStateMachine` — состояния idle / walk / crouch / jump / fall / ride / indoors /
  dead. Вынесена из узла в отдельный класс: принимает снимок ввода и факты о теле,
  возвращает состояние. Поэтому тестируется без сцены и физики — 14 тестов.
- `Otto` (`CharacterBody3D`) — гравитация, дискретное аркадное движение, смена формы
  коллизии при приседании; Z заперт. Фигура — модель пака на скелете, присед
  укладывается под пулю агента, и это проверяется по вершинам. Камера — `SideCamera`
  поверх правила `CameraBounds`: границы здания и сглаживание; с M17 наклонена на
  десять градусов сверху, и пол виден полосой (ADR-0023). В кадре 3.67 этажа, как
  у оригинала (ADR-0026, решение 4).
- `GreyboxLevel` (`Node3D`) — здание собирается в рантайме по `BuildingPlan`
  строителями: оболочка (`BuildingShell`), рёбра (`BuildingRibs`), шахты
  (`BuildingShafts`), окружение (`BuildingScenery`). Плоскость правил переводится
  в сцену через `WorldSpace`. Над зданием — крыша отдельным уровнем
  (`BuildingRules.ROOF`); Otto съезжает на неё по тросу, с неё и начинается спуск.
- Ввод: клавиатура (стрелки/WASD, пробел/Z, X/J, Esc) и геймпад (крестовина, стик,
  A, X, Start). Клавиши читаются по `physical_keycode` — работает на любой раскладке.
- `ElevatorMotion` — ход кабины отдельным классом: управление изнутри, автономная
  езда от этажа к этажу с паузой, границы шахты, совпадение с полом этажа. 19 тестов
  без сцены и физики.
- `ElevatorCar` — кабина на `AnimatableBody3D` с `sync_to_physics`. Переносит того,
  кто стоит на крыше, и давит того, кто оказался под днищем.
- `Escalator` — площадки сверху и снизу, посадка нажатием, поездка без управления.
- `ShaftHazards` — правила гибели в шахте отдельно от физики, с тестами.
- Контекстный ввод: в кабине «вверх/вниз» ведут её, присед выключен.
- `Door` и `DoorVisit` — за красной дверью документ на 500 очков, внутри можно пересидеть
  до пяти секунд; `DocumentRoute` возвращает к верхней несобранной двери, если Otto вышел
  из здания раньше времени.
- `Gun` и `Bullet` — пистолет с тремя пулями на экране, удар ногой в прыжке. Пуля одна
  на обоих стрелков.
- `Enemy` и `EnemyBrain` — агенты выходят из дверей, бродят по этажу, стреляют
  стоя, присев или лёжа и уворачиваются от пуль Otto: от высокой приседают, от
  низкой ложатся. Otto в тени замечают только ближе 1,8 м, за дверью и за стеной
  не видят вовсе. Злость у каждого своя и растёт со временем; от неё замах, пауза,
  поза выстрела и шанс увернуться. Ездят в кабинах пассажирами, кабина давит их
  днищем (300 очков).
- **Числа боя — в `Arcade`:** таблица правил аркадного ROM в тиках оригинала —
  сложность, злость, выпуск, выстрел, увёртка, скорости (ADR-0027, решение 1).
  `BuildingRules` несёт навык здания, от которого таблица считает остальное, и
  ручные потолки для тестов.
- **Агентов в здании три-четыре разом, как в ROM:** выходят из случайной двери
  на этаже Otto, этажом выше или ниже (`AgentSpawn`); дверь вплотную к Otto не
  выпускает (`AGENT_SAFE_RELEASE`). Случайность посеяна сидом здания.
- `Lamp`, `LampFall`, `FloorLighting` — ламп на этаже одна–три по ширине, на тёмных
  этажах карты ни одной; сбитая падает, убивает агента под собой (300 очков) и
  гасит свою зону насовсем — зона это ближайшая лампа, соседние горят.
- `BuildingRules`, `BuildingPlan`, `BuildingRoute` — здание на 30 этажей плюс крыша
  раскладывается по правилам и сиду: шахты **перехлёстываются** и их тем больше, чем
  ниже этаж — от одной наверху до пяти на дне; эскалаторы стоят полосой у порога и
  на разрывах перехлёста. Двери, красные двери и тёмные этажи — по таблицам ROM,
  документов от пяти до десяти по навыку (ADR-0028). Сид — номер здания с солью
  партии, в тестах и у бота соль нулевая. Проходимость проверяется тестом, а путь
  по зданию — тем же графом, которым ходит бот (`BuildingRoute.walkable`).
- **Силуэт порогом:** двадцать верхних этажей — узкая башня в 7 мест и 15.6 м,
  влезает в кадр (23.5 м при 16:9) целиком; ниже стилобат во все 17 мест и 33.6 м,
  в полтора кадра. Наверху и дверей меньше. Места нумеруются глобально и идут
  шагом 1.8 м, поэтому шахта стоит в одном столбце на всех своих этажах
  (ADR-0014, ADR-0024, ADR-0026).
- **Этаж бывает разрезан:** проёмом шахты и глухой внутренней стеной. Сквозь стену
  не пройти и не выстрелить, и агент за ней Otto не видит; сквозь проём переходят
  по стоящей кабине, как в оригинале.
- **Агенты выходят рядом с игроком:** дверь отдаёт своего, когда её этаж попал в
  полосу видимых, и убирает ушедшего далеко. Полоса считается по кадру правил, а не
  по сглаженной камере, — иначе выпуск зависел бы от частоты кадров.
- **Возвращение в игру:** полторы секунды неуязвимости с миганием и место подальше
  от живых агентов — иначе Otto воскресал под тем же стволом.
- `GameState` и `Alarm` — три жизни, респавн, Game Over, бонус на выходе, следующее
  здание со злее агентами, сирена за медлительность.
- Свет: у лампы конус с мягкой тенью и слабая заливка, оба уходят вместе с ней;
  `Atmosphere` — общий тон палитры, отражения в полированном полу (SSR), SSAO,
  туман намёком, свечение с порога, ACES. Над крышей один источник, пуля несёт
  вспышку. Горят только лампы и столбы света шахт видимых этажей (`VisibleFloors`).
  За зданием город (`CityBackdrop`, `CityDetails`) — окна эмиссией, без
  источников; погода по сиду (`Weather`): ясная ночь, туман или дождь с молниями
  (`Lightning`).
- Качество графики (`Graphics`): четыре уровня до «Ультра», при первом запуске
  уровень выбирает замер (`QualityProbe`); режимы окна и масштаб рендера —
  `DisplayModes`; виньетка слоем под HUD (`Vignette`).
- Вид: `GreyboxLook` — материалы, огоньки (табло дверей, индикаторы кабины,
  вывеска выхода) и обводка актёров; `BuildingRibs` — торцы плит, плинтус,
  пилястры; `BuildingFinish` — фактуры стен, шахты и крыши. Здание — отель или
  офис (`BuildingIdentity`) с неоновой вывеской на углу (`VerticalSign`), этажи
  обставлены моделями паков (`BuildingDressing`, `PropCatalog`), машина у выхода
  своя в каждом здании (`ExitCar`). Актёры — `FigureRig` поверх `.glb` из
  `tools/build_actors.py`: модели пака Quaternius, клипы пака и позы кодом из
  `FigurePoses`, выбор позы — `ActorPose` (ADR-0032).
- Звук: 17 эффектов, тема здания и мотив тревоги — всё синтезировано
  `tools/render_audio.py`; три шины (Master, Music, SFX) и позиционные источники.
- Интерфейс: `Menu` на шесть страниц, `Hud`, `GameSettings` и `Records` — меню,
  пауза, настройки, таблица рекордов и экран управления; два языка по локали системы.
- Скриншоты: `python tools/capture.py <веха>` проходит маршрут вехи; F12 в игре. Бот:
  `tools/playthrough.gd` с `--trace` печатает, где Otto, что он нажал и где кабина.

## Текущая веха — M22b · Меню

Решения — [ADR-0035](adr/0035-menu.md). Замечание пользователя: игра смотрится
современно, а меню — «убого».

**DoD:** меню можно поставить рядом с кадром игры, и они выглядят одной игрой.

### Что показал разбор

- **За главным меню пусто:** `Main._open_menu` выбрасывает здание, под
  затемнением — серый градиент.
- **Город живёт сам:** `CityBackdrop` — свой `SubViewport` на слое −1, от
  здания ему нужны только высота, ширина и земля; камеру он ведёт по текущей.
- **Шрифтов два:** Pixellari в теме меню, на вывеске фасада, табличках этажей,
  табло шахт и табличках дверей; Exo 2 — в HUD.
- **Мелко и крупно разом:** подписи настроек — кегль 12, выпадающие списки —
  48 из темы.

### Вопросы вехи

- **Фон** — живая сцена: ночной город; на паузе — игра, размытая.
- **Название** — неоновая вывеска.
- **Шрифт** — Exo 2 везде, Pixellari уходит.
- **Анимация** — переходы, подсветка и звуки.

### План вехи

- [x] Вопросы, ADR-0035, план в EPIC
- [x] Общий стиль: `NeonStyle` — плашка, шрифт нужного веса, подпись; HUD
      перешёл на него. Тема `ui_theme.tres` удалена — меню собирает стиль кодом
- [x] Фон: `MenuStage` — город со своей ортокамерой, втрое шире игрового кадра
      (при игровом окна вставали во весь экран), камера качается вдоль улицы;
      на паузе и в конце партии — игра, размытая шейдером (`menu_blur`)
- [x] Вывеска `NeonTitle`: три слоя ореола, трубка и раскалённая середина,
      буква «T» мигает узором; свечение нарисовано — 2D-свечения у движка нет
- [x] Страницы: колонка слева, пункты `MenuRow` — кнопка, переключатель
      «‹ значение ›», громкость полосой, флажок; рекорды и управление сеткой;
      Esc и B — назад с подстраниц
- [x] Анимация: колонка въезжает слева, выбранный пункт загорается за 0.14 с;
      звуки `ui_move`, `ui_select`, `ui_back` — синтезом
- [x] Pixellari → Exo 2 в сцене: вывеска, таблички этажей (высота цифры
      перемерена по глифам — 0.71 кегля), табло шахт, таблички дверей; шрифт с
      лицензией удалён
- [x] Тесты: `test_menu` — каждая страница собирается и отдаёт фокус, состав
      настроек, размытие только над игрой, переключатели, сцена без здания,
      ни одной ссылки на Pixellari; `check.ps1` зелёный: 61 файл за 166 с
- [x] Кадры: `ui_shot` до и после (`screens/menu_before`, `menu_after`),
      `capture.py M22b`, сравнение с оригиналом — вывеска и таблички на Exo 2
      читаются, цифры этажей того же характера, что красные табло аркады
- [x] Авторевью: 15 находок, исправлены 12. Esc на настройках, открытых с
      паузы, возвращал на паузу и тут же её снимал — тест; смена языка уводила
      фокус на громкость; снимки меню шли посреди въезда колонки; цвет пункта,
      заданный после сборки, не применялся; громкость у края щёлкала впустую и
      не листалась удержанием; вывеска мигала невидимой; «Esc — назад» висело
      там, где Esc назад не ведёт. Оставлены: имя проверки CI сменила матрица —
      правится в защите ветки на GitHub; мипмапы Exo 2 — в долг
- [x] `check.ps1` зелёный после правок ревью: 61 файл за 166 с
- [x] README
- [ ] PR

## Задача перед вехой — долг после сверки

Решение пользователя: закрыть найденное сверкой и мелкий старый долг, правило
ухода агента по ROM и утечку. Переназначение клавиш, демо-режим, качание лампы,
темнота в поздних зданиях и трассировка лучей остаются в долге — это вехи.

- [x] **Типы проверяет движок:** `untyped_declaration` в `project.godot` —
      ошибка разбора; всплыли семь переменных цикла в четырёх тестах
- [x] **Архив релиза с лицензиями:** Exo 2 и `CREDITS.md` едут с игрой; тест —
      у каждого шрифта `assets/fonts/` лицензия в архиве
- [x] **Бонус по ROM:** 1000 × номер здания, но не больше чем за десятое
      (`Arcade.building_bonus`, @5793)
- [x] **Уход агента по ROM:** 80 px и этаж ROM с восьмого (`Arcade.agent_leaves`);
      прежде уходил с любого этажа. Бот не заметил: 0, 1, 1 смерть на сидах
      1–3, как и было. Третье условие ROM — «кроме двадцатого» — не взято: с
      ним сид 2 стоил 10 смертей вместо одной, дважды подряд; вопрос 9
- [x] **Устаревшие комментарии** — семь мест; ссылки эскалатора на ADR-0004
- [x] **Мелочи:** действия `restart`/`quit_game` удалены; четыре копии ожидания
      спуска сведены в `GreyboxLevel.wait_for_the_landing`; CI гонит тесты
      матрицей на трёх машинах (`run_tests.py --part K/N`)
- [x] **Утечка ObjectDB — не наша.** Утекает не здание, а воспроизведение темы
      `theme.ogg`: выход застаёт его у звукового сервера, пока микшер не успел
      его снять. В headless так всегда — фиктивный драйвер микшер не крутит; с
      настоящим (WASAPI) — только когда выход идёт сразу за стартом музыки, как
      в `ui_shot`. Игра на 300 кадров с окном не теряет ничего, а остановка
      звука на выходе не помогает — это гонка движка, правки в коде нет
- [x] `check.ps1` зелёный: 60 файлов за 165 с
- [ ] PR

## Сверка документации

Решение пользователя после M22: каждый `.md` проекта, включая README, по каждому
предложению и утверждению. Ветка `docs/sweep`.

### Как сверяем

- **Документ описывает то, что есть.** Если неправ, похоже, код — документ
  пишет, как есть сейчас, а расхождение уходит в долг ниже с пометкой «правка за
  кодом». Код в этой ветке не трогаем.
- **ADR не переписываем:** это запись того, что решили тогда. Отменённому или
  дополненному ADR — строка статуса в шапке со ссылкой на того, кто отменил;
  ссылки, имена и числа, которые ADR называет текущими, — сверяем.
- **`milestones.md` — каждое утверждение** против кода той вехи, по git.
- **Закрытый долг из STATUS уходит;** разборы, которые стоит помнить, —
  в `milestones.md` к своей вехе.

### План

- [x] Вопросы, план
- [x] README, CLAUDE.md, CHANGELOG, CREDITS, `conventions.md`, `testing.md`,
      `reference/arcade-rom.md`. Главное: CLAUDE.md обещал проверку типов
      линтером, которой нет; CHANGELOG описывал 2D-сборку и дату несостоявшегося
      релиза; высоты пуль в `arcade-rom.md` стояли в клетке этажа, а не над полом
- [x] STATUS.md и EPIC.md: «Что работает» переписан под 3D и ROM, размеры
      здания по `Proportions`, закрытые вопросы и шестнадцать пунктов закрытого
      долга сняты; в EPIC M20 стояла несделанной, хотя влита
- [x] ADR 0001–0034: шапки статуса у всех, отменён целиком только ADR-0002;
      около двадцати неверных ссылок на пункты других ADR, ссылки в синтаксисе
      докстрок Godot заменены именами
- [x] `milestones.md` по вехам против git: около сорока правок, материал M18b,
      попавший в M18a, перенесён; дописаны вехи M1, M5a, M9, M10 и M12
- [x] Расхождения с кодом — в долг ниже: лицензии в архиве релиза, проверка
      типов, бонус без потолка, устаревшие комментарии
- [x] `check.ps1` зелёный: 60 файлов за 164 с
- [ ] PR

## Последняя веха — M22 · Грейдинг и полировка

Решения — [ADR-0030](adr/0030-grading-and-quality.md) и
[ADR-0034](adr/0034-ultra-and-auto-quality.md).

**DoD:** кадр можно поставить рядом с референсом и говорить о разнице в ремесле,
а не в возможностях.

### Вопросы вехи

- **«Ультра»:** объёмный свет ламп, отражённый свет SSIL, тени высокого
  разрешения. SDFGI — нет: свет потёк бы сквозь стены разреза.
- **Сглаживание** — в уровнях, без отдельного пункта.
- **Уровень при первом запуске** — по замеру кадра.
- **Тон** — доводим нуар.

### План вехи

- [x] Вопросы, ADR-0034, план в EPIC
- [x] «Ультра» в `Graphics`, настройках и переводах: SSIL, свет ламп, шахт и
      неона в объёмном тумане втрое, сетка тумана вдвое мельче, атлас теней 8192
      с мягким фильтром. Гуще туман не сделан: на пробе дымка ложилась на Otto
- [x] Сглаживание по уровням на корневом окне: FXAA, MSAA ×2, ×2, ×4. TAA снят
      по кадрам — размывал обводку актёров
- [x] Уровень при первом запуске по замеру (`QualityProbe`): на вступлении
      первого здания, с «Ультра» вниз по медиане GPU ≤ 12 мс, не дольше 8 с
- [x] Виньетка слоем под HUD (`Vignette`); дизеринг включён в `project.godot`
      — дополнение ADR-0002 записывало его с M20, а настройки не было
- [x] `layout_shot --floor-only --quality=N` — один этаж на каждом уровне
- [x] Тон по кадрам: три набора (`layout_shot --tone=N`), взят второй — холод
      в тенях и тепло в свете разведены сильнее, мрамор не выбеливается
- [x] HUD — неон-нуар (решение пользователя): плашки с кромкой в цвет вывески,
      шрифт Exo 2 (OFL), значки вместо слов — папки документов, силуэты
      жизней; по центру имя здания и этаж Otto. Папок — сколько документов в
      здании (по ROM 5–10): на первой сборке их было пять навсегда
- [x] Мерцание (замечание пользователя): `tools/flicker_shot.tscn` снимает 60
      кадров неподвижного этажа с вертикальной синхронизацией и строит карту
      разницы. Кромки дверей, перекрытий и стен мерцали от камеры: сглаживание
      экспонентой не доходило до стоящей цели и сдвигало кадр на доли пикселя
      — теперь в полумиллиметре камера встаёт ровно. Остальное на карте
      задумано: кабины, дыхание Otto, табло, мигающая буква вывески. Разрыв
      кадра в прогонах — замер `light_bench`, он выключает синхронизацию
- [x] Разрешение (вопрос пользователя, `DisplayModes`): окно, без рамки или
      полный экран в родном разрешении; размер окна — стандартные до 3840×2160,
      только те, что влезают на монитор; масштаб 3D-рендера 100/77/67/50 % через
      FSR, интерфейс не задет. Старый флажок «полный экран» читается режимом
- [x] Город детальнее (`CityDetails`): верхи домов — уступы, шпили, баки,
      антенны; красные огни мигают каждый в своей фазе шейдером; неоновые вывески
      на любой высоте фасада; у трети домов горит целый этаж; зарево улиц. Без
      источников света. Раскладка кварталов по сиду не сдвинулась
- [x] Погода богаче: ясной ночью звёзды и луна, в тумане плывут полосы дымки,
      в дождь — молнии (`Lightning`): серии вспышек, небо и погасшие стёкла
      домов загораются, воздух здания светлеет, изредка виден разряд. Гром — M23
- [x] Бюджет по всему зданию (`light_bench --whole`), худший кадр GPU:
      низкое 1.1 мс, среднее 1.7, высокое 3.7, «Ультра» 8.6 из 16.6
- [x] Уборка 2D: `tools/palette.py` удалён — девять цветов актёров переехали в
      `build_actors.py`; у `BuildingPalette` сняты поля без читателей (`lit`,
      `shaft_light`), тест темноты сверяет тон с настоящим светом лампы
- [x] Тесты: уровни качества и замер (`test_graphics`), режимы окна, папки
      HUD, камера в покое, город и молния; `check.ps1` зелёный: 60 файлов за 164 с
- [x] Кадры: `capture.py M22`, уровни рядом (`layout_shot --floor-only
      --quality=N`), тон (`--tone=N`), погода на крыше (`--roof-only`), карта
      мерцания; сравнение с оригиналом
- [x] README
- [x] Авторевью: 15 находок, исправлены 14. Разряд молнии не рисовался вовсе
      — треугольники отсекались как задние; замер на исходе времени оставлял
      неизмеренный уровень, и самая слабая карта получала «Ультра»; замер
      перебивал выбор игрока и жил дольше здания — второй запускался поверх
      первого; окно прыгало в центр при любой смене настроек; окно FullHD на
      FullHD-мониторе уходило заголовком за край и низом под панель задач;
      подписи HUD не переводились при смене языка; замер не видел кадра города —
      теперь складывает время окна и вида города. Оставлено как есть: заливка
      ламп светит в туман на четверть на всех уровнях — так подобран вид по кадрам
- [x] `check.ps1` зелёный после правок ревью
- [x] PR #40

## Что дальше: после пивота

**Сверка документации** — после M22, решение пользователя: каждый `.md` в
проекте, включая README, по каждому предложению и утверждению. Затем **M23 —
звуковое сопровождение** (решение пользователя: звук кажется примитивным) —
треки и звуки под смысл игры, с лицензиями. Лидерборд сдвинулся в M24. План — в
[EPIC.md](EPIC.md).

## Переезд в 3D — завершён

Решение — [ADR-0019](adr/0019-3d-pivot.md): 3D-сцена с ортокамерой сбоку вместо
2D-кадра из спрайтов, логика игры — без изменений. Пивот шёл вехами M15–M22 и
закончен; 2D-сборка осталась в истории (`d5774df`). Правила пивота — каждая веха
играбельна, читаемость аркады важнее кинематографичности, механика не живёт в
узле — и проба `tools/look3d.gd`, с которой всё началось, записаны в том же ADR.

## Принятые решения

| | |
|---|---|
| [ADR-0001](adr/0001-tech-stack.md) | Godot 4.7 и типизированный GDScript, без C# |
| [ADR-0002](adr/0002-visual-target.md) | HD пиксель-арт с динамическим светом; отменён ADR-0019 |
| [ADR-0003](adr/0003-no-docker.md) | Docker не используется; уместен разве что для бэкенда лидерборда |
| [ADR-0004](adr/0004-elevator-mechanics.md) | Механика лифтов, шахт и эскалаторов по оригиналу |
| [ADR-0005](adr/0005-doors-and-documents.md) | Двери, документы и выход из здания |
| [ADR-0006](adr/0006-combat-and-enemies.md) | Бой, враги и разделение вехи M4 |
| [ADR-0007](adr/0007-lamps-and-darkness.md) | Лампы, темнота и неуязвимость на эскалаторе |
| [ADR-0008](adr/0008-building-generation.md) | Здание генерацией, разделение M5, тревога |
| [ADR-0009](adr/0009-game-loop-and-alarm.md) | Игровой цикл, тревога и рост сложности |
| [ADR-0010](adr/0010-lighting-and-atmosphere.md) | Свет, темнота, город за окнами |
| [ADR-0011](adr/0011-asset-pipeline.md) | Арт-пайплайн: Blender на актёров, код на окружение, разделение M7; спрайты отменены ADR-0019 и ADR-0022 |
| [ADR-0012](adr/0012-sound-and-interface.md) | Звук синтезом, свой мотив, шрифт с кириллицей, разделение M8 |
| [ADR-0013](adr/0013-release-and-versioning.md) | Релиз, версии и теги |
| [ADR-0014](adr/0014-building-architecture.md) | Крыша, силуэт здания и выпуск агентов |
| [ADR-0015](adr/0015-round-palette-and-roof.md) | Палитра раунда и крыша по Spectrum-версии |
| [ADR-0016](adr/0016-combat-balance.md) | Баланс боя и уклонение агентов; модель злости заменена ADR-0027 |
| [ADR-0017](adr/0017-spectrum-palette-and-shafts.md) | Спектрумовская палитра, видимая шахта и крыша с тросом |
| [ADR-0018](adr/0018-native-fullhd.md) | Нативный FullHD, детализация ассетов, пропорции и предел шахты |
| [ADR-0019](adr/0019-3d-pivot.md) | Переезд на 3D-узлы; отменяет ADR-0002 и половину ADR-0010 и ADR-0011 |
| [ADR-0020](adr/0020-agent-doors.md) | Дверь агента: телеграф, проём и неуязвимость |
| [ADR-0021](adr/0021-3d-greybox.md) | 3D-greybox: плоскость игры, глубина комнаты, порядок переезда |
| [ADR-0022](adr/0022-actors-rig.md) | Актёры: риг из Blender, анимация кодом, обводка вместо свечения |
| [ADR-0023](adr/0023-light-and-readability.md) | Свет: наклон камеры, зоны ламп, рёбра, огоньки; темнота решает видимость |
| [ADR-0024](adr/0024-building-geometry.md) | Геометрия здания: сетка мест, силуэт порогом, перехлёст шахт, стены, двухэтажная кабина |
| [ADR-0025](adr/0025-shafts-escalators-and-riders.md) | M18b: эскалатор конструкцией, свет шахты, двухэтажная пара, агенты в кабинах |
| [ADR-0026](adr/0026-proportions.md) | M18c: пропорции по оригиналу — растёт содержимое, агент ростом с Otto, 3.67 этажа в кадре, лампа из кабины |
| [ADR-0027](adr/0027-rom-combat.md) | M18d: бой по правилам ROM — сложность, злость агента, 3–4 агента, низкий выстрел, скорости и прыжок, давка кабиной |
| [ADR-0028](adr/0028-building-by-the-map.md) | M18e: здание по карте — двери ROM на ширину экрана, красные полосами по навыку, тёмные этажи 11–15, соль партии |
| [ADR-0029](adr/0029-city-weather-dressing.md) | M19: город в своём перспективном виде, погода по сиду, обстановка-декор, крыша силуэтом, палитра материалами |
| [ADR-0030](adr/0030-grading-and-quality.md) | M22: нуар-грейдинг, город в расфокусе, виньетка, дизеринг, три уровня качества, бюджет на всём здании |
| [ADR-0031](adr/0031-scene-detail.md) | M20: детализация — современный лифт, крыша с техникой, антенной и неоном, этажи, окна города сеткой |
| [ADR-0032](adr/0032-actor-models.md) | M21: Otto и агенты из пака Quaternius — клипы пака и позы кодом, федора и очки агента, машина из Cars Pack |
| [ADR-0033](adr/0033-dressing-from-packs.md) | M21b: отель или офис жребием, вертикальная вывеска, обстановка и крыша моделями паков, фактуры стен, стальная шахта |
| [ADR-0034](adr/0034-ultra-and-auto-quality.md) | M22: уровень «Ультра», сглаживание внутри уровней, уровень при первом запуске по замеру, нуар; дополняет ADR-0030 |

Прочее: базовый вьюпорт 1920×1080 (ADR-0018 отменил 640×360 из ADR-0002,
ADR-0019 отменил и сам ADR-0002); растяжение `canvas_items` обслуживает только HUD
и меню, 3D рисуется в разрешении окна с масштабом рендера из настроек; приседание останавливает движение
(как в оригинале), прыжок из приседа запрещён. **Coyote time и буфер прыжка не добавляем:**
вопрос был отложен до конца M2, а формула проекта — механика 1983 года, и в оригинале
ни того, ни другого нет.

## Открытые вопросы

| # | Вопрос | Блокирует |
|---|---|---|
| 8 | Дистанция 1,8 м, с которой агент замечает Otto в тени (ADR-0023, решение 8), выбрана, а не замерена: прогон бота на шести сидах разницы не показал и показать не мог — **бот не умеет гасить лампы**, а значит не проверяет ровно то, ради чего механика заведена | — |
| 4 | Не подтверждены сверкой ни с ROM, ни с источниками: кабина игрока встаёт между этажами (ADR-0004, пункт 6); правила возвращения в игру — неуязвимость 1.5 с и место подальше от агентов; безопасное расстояние двери 2.88 м (`AGENT_SAFE_RELEASE`) | — |
| 9 | ROM не даёт отставшему агенту уйти в дверь на двадцатом этаже (@041F). У нас с этим условием сид 2 теста боя стоил 10 смертей вместо одной — агенты копились на двадцатом. Взять ли его, подняв порог смертей, или оставить отступлением: нужно понять, что двадцатый значит в раскладке аркады | баланс боя |

Закрытые: 3 — площадка, только GitHub Releases
([ADR-0013](adr/0013-release-and-versioning.md), пункт 1); 5 — кадры анимации
(снят вместе со спрайтами); 6 и остальное из 4 — числа боя, красные двери,
затемнение и тревога по ROM ([ADR-0027](adr/0027-rom-combat.md),
[ADR-0028](adr/0028-building-by-the-map.md)); 7 — низкий выстрел по ROM
([ADR-0027](adr/0027-rom-combat.md), решение 3).

## Долг и заметки

- **Exo 2 импортирован без мипмапов,** а с M22b им написаны и надписи в сцене —
  таблички, табло, вывеска. Мелкая или дальняя надпись может мерцать.
  Включить `generate_mipmaps` — значит поменять вид всего текста, в том числе
  HUD и меню; решать по кадрам (авторевью M22b).
- **Стрелок ▲▼ на табло шахт нет ни в одном шрифте** — ни в Exo 2, ни в
  прежнем Pixellari: их рисует системный запасной шрифт, и на машине без него
  стрелка выйдет пустым квадратом. Нужен шрифт со стрелками или стрелка
  геометрией. Найдено при переходе на Exo 2 (M22b).
- **Трассировка лучей** (вопрос пользователя, 2026-09-24). В Godot 4.7 только
  низкоуровневый Vulkan RT в `RenderingDevice`, готовых RT-теней, отражений и GI
  нет; форк NVIDIA — отдельная сборка движка. Решено: «Ультра» M22 собрана
  средствами движка (SSIL, объёмный свет, тени 8192; SDFGI — нет, ADR-0034), а RT
  опцией — когда он появится в основной ветке. Проверять при каждом обновлении
  движка.
- **Качание лампы после выстрела не заведено:** сбитая падает сразу (ADR-0007),
  подвес — шнур — есть (ADR-0023, «Чего в вехе нет»).
- **Темнота не слабеет в поздних зданиях**, хотя вторичные источники это описывают
  («in later levels, it doesn't work as well or at all»). В ROM темнота на агентов
  не влияет вовсе, и наша темнота — сознательное расхождение (ADR-0027,
  решение 9); ослабление с навыком было бы правилом сложности рядом с
  `Arcade.difficulty` — ADR-0010, пункт 7.
- Новый `class_name` не виден движку, пока проект не переимпортирован: сцена,
  которая на него ссылается, молча возвращает `null`. Лечится
  `python tools/godot_check.py` — он импортирует проект.
- **Переназначения клавиш нет.** Экран управления показывает раскладку, но не
  меняет её: настоящее переназначение — это ещё и геймпад, и разбор конфликтов,
  и сохранение схемы (ADR-0012, пункт 10). Отложено после релиза.
- **Демо-режима нет** (в автомате он крутится между партиями). Нужен бот, который
  играет сам; такой у нас есть только в тестах — ADR-0012, «Чего в вехе нет».

## Что было раньше

Итоги пройденных вех и находки авторевью — в [`milestones.md`](milestones.md).
Здесь только то, что описывает проект сегодня: иначе файл, который читают
первым в новой сессии, тонет в истории.
