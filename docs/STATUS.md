# Статус разработки

Обновляется в каждом коммите, который трогает `src/`, `tests/` или `project.godot`.
Это проверяет хук `status-updated`.

| | |
|---|---|
| **Обновлено** | 2026-09-20 |
| **Текущая веха** | M14 — дверь открывается по-настоящему |
| **Ветка** | `feat/m14-doors` |
| **Состояние** | Веха готова: 334/334 зелёные, авторевью, скриншоты и README сделаны. **Осталось: PR** |
| **Впереди** | **Проект переезжает в 3D** с M15 — [ADR-0019](adr/0019-3d-pivot.md) |

## Где мы сейчас

Пройдены **M0–M6**: фундамент, движение, лифты и эскалаторы, двери и документы,
бой и лампы, здание генерацией, игровой цикл, свет и атмосфера. Всё это в `main`.

Играется партия: Otto спускается с крыши тридцатиэтажного здания, собирает пять
документов из красных дверей, отбивается от агентов, уходит в выход и попадает
в следующее здание, где агенты злее. Провозился — включается сирена. Этаж светел,
пока на нём горит лампа; сбитая гасит его насовсем.

Пройдена **M7 целиком**: игра собрана из ассетов. Окружение рисует генератор на
Python, актёры и машина сняты с лоу-поли модели в Blender; у всего есть нормаль и блик,
и свет из M6 ложится на рельеф. Цветных коробок в кадре не осталось.

Пройдена **M8a**: игра звучит — шаг, выстрел, лифт, двери, лампы, смерти, тема
здания и мотив тревоги.

Пройдена **M8b**: игра запускается из меню и возвращается в него. Меню, HUD,
настройки, рекорды и два языка на месте.

Пройдена **M9**: сборки под Windows и Linux, версии, теги и workflow релиза.
Тег ещё не ставился — первым должен уехать билд уже с M10.

Пройдена **M10**: крыша отдельным уровнем, ступенчатый силуэт, выпуск агентов
рядом с игроком.

Пройдена **M11**: бой сбалансирован. Оси роста сложности как в оригинале, агенты
уклоняются от пуль, живых в здании не больше восьми, числа боя — в правилах здания.
Бот проходит здание с агентами на трёх жизнях, и это проверяется тестом.

Пройдена **M12**: визуал по версии для ZX Spectrum — палитра раунда, видимая
шахта, машинное отделение на крыше и спуск по тросу.

Пройдена **M13**: нативный FullHD и качественные текстуры — мир втрое крупнее,
ассеты перерисованы с деталью, пропорции переразмечены, предел шахты виден в кадре.

M12 и M13 влиты в `main` (PR #24).

Идёт **M14** — последняя веха на 2D: дверь агента открывается по-настоящему.

## Что работает

- `OttoStateMachine` — состояния idle / walk / crouch / jump / fall / dead. Вынесена
  из узла в отдельный класс: принимает снимок ввода и факты о теле, возвращает
  состояние. Поэтому тестируется без сцены и физики — 14 тестов.
- `Otto` (`CharacterBody2D`) — гравитация, дискретное аркадное движение, смена формы
  коллизии при приседании, камера с границами.
- `GreyboxLevel` — здание собирается в рантайме; геометрия одета в ассеты окружения
  (`SpriteTextures`), свет из M6 ложится на их нормаль. Над зданием — крыша
  отдельным уровнем (`BuildingRules.ROOF`), с неё и начинается спуск.
- Ввод: клавиатура (стрелки/WASD, пробел/Z, X/J, Esc) и геймпад (крестовина, стик,
  A, X, Start). Клавиши читаются по `physical_keycode` — работает на любой раскладке.
- `ElevatorMotion` — ход кабины отдельным классом: управление изнутри, автономная
  езда от этажа к этажу с паузой, границы шахты, совпадение с полом этажа. 17 тестов
  без сцены и физики.
- `ElevatorCar` — кабина на `AnimatableBody2D` с `sync_to_physics`. Переносит того,
  кто стоит на крыше, и давит того, кто оказался под днищем.
- `Escalator` — площадки сверху и снизу, посадка нажатием, поездка без управления.
- `ShaftHazards` — правила гибели в шахте отдельно от физики, с тестами.
- Трёхэтажный grey-box: перекрытия с проёмами под шахту и эскалатор, нижний этаж
  сплошной как дно шахты.
- Контекстный ввод: в кабине «вверх/вниз» ведут её, присед выключен.
- `Door` и `DoorVisit` — за красной дверью документ на 500 очков, внутри можно пересидеть
  до пяти секунд; `DocumentRoute` возвращает к верхней несобранной двери, если Otto вышел
  из здания раньше времени.
- `Gun` и `Bullet` — пистолет с тремя пулями на экране, удар ногой в прыжке. Пуля одна
  на обоих стрелков.
- `Enemy` и `EnemyBrain` — агенты выходят из дверей, стреляют, в темноте видят ближе,
  уклоняются от пуль (стоя 26 px, на колене 17, лёжа 8). Злость растёт от здания
  к зданию и от тревоги, с общим потолком; растит она скорострельность и скорость
  пули, но не дальность — как в оригинале.
- **Числа боя — в `BuildingRules`:** дальность, пауза, замах, скорость пули, пороги
  уклонения и потолок живых агентов. В сцене агента их больше нет, поэтому их можно
  растить от здания к зданию (ADR-0016, пункт 5).
- **Живых агентов не больше восьми:** уровень выпускает за кадр одного и выбирает
  дверь поближе к игроку. Раньше на нижних этажах их набиралось до восемнадцати.
- `Lamp`, `LampFall`, `FloorLighting` — сбитая лампа падает, убивает агента под собой
  (300 очков, самый дорогой способ) и гасит этаж насовсем.
- `BuildingRules`, `BuildingPlan`, `BuildingRoute` — здание на 30 этажей плюс крыша
  раскладывается по правилам и сиду: шахты не сквозные, на стыках эскалаторы, пять
  документов разнесены по высоте. Проходимость раскладки проверяется тестом.
- **Силуэт ступенями:** наверху 5 мест и 720 px ширины, в середине 7 и 1000, внизу
  9 и 1280. Наверху и дверей меньше. Места нумеруются глобально, поэтому шахта
  стоит в одном столбце на всех своих этажах (ADR-0014).
- **Агенты выходят рядом с игроком:** дверь отдаёт своего, когда её этаж попал в
  полосу видимых, и убирает ушедшего далеко. В кадре единицы вместо 55 по зданию.
- **Возвращение в игру:** полторы секунды неуязвимости с миганием и место подальше
  от живых агентов — иначе Otto воскресал под тем же стволом.
- `GameState` и `Alarm` — три жизни, респавн, Game Over, бонус на выходе, следующее
  здание со злее агентами, сирена за медлительность.
- Свет: `AreaLight`, `VisibleFloors`, `LightTextures`, `Skyline`, `postprocess.tscn` —
  два источника на этаж, столб света в шахте, вспышки выстрелов, город за окнами,
  свечение и виньетка. Горят только источники видимых этажей.
- Ассеты окружения: генератор `tools/render_env.py` рисует диффуз, нормаль и блик из
  одной геометрии, `SpriteTextures` собирает из них `CanvasTexture`.
- Звук: 17 эффектов, тема здания и мотив тревоги — всё синтезировано
  `tools/render_audio.py`; три шины (Master, Music, SFX) и позиционные источники.
- Интерфейс: `Menu` на шесть страниц, `Hud`, `GameSettings` и `Records` — меню,
  пауза, настройки, таблица рекордов и экран управления; два языка по локали системы.
- Скриншоты: `python tools/capture.py M7a` проходит маршрут вехи, `tools/light_shot.tscn`
  снимает свет по состоянию и меряет кадр; F12 в игре.

## Последняя веха — M13 · Нативный FullHD и качественные текстуры

Решения — [ADR-0018](adr/0018-native-fullhd.md).

**DoD:** игра идёт в 1920×1080 без растягивания, и на глаз она детальнее, а не
просто крупнее: на стоп-кадре видно кирпич, филёнку и патрон лампы.

### Что показала проба

Сделана до вехи, чтобы не перерисовывать вслепую:

- Ассеты окружения описаны прямоугольниками в единицах мира, поэтому рендер
  «втрое крупнее» даёт **пиксель в пиксель то же самое**.
- Прибавка есть у актёров: они приходят рендером из Blender, и в 3× у них
  появляется настоящая полутень.
- Перерисованный с деталью ассет выигрывает сразу.

Отсюда состав вехи: разрешение — повод, работа — текстуры.

### Сделано в вехе

- **Вьюпорт 1920×1080, мир втрое крупнее.** Высота этажа, ширина здания, рост
  актёров, скорости, прыжок — всё в пикселях и всё ×3. Кадр показывает то же,
  что и раньше, но втрое плотнее.
- **Оба генератора умеют масштаб:** `render_env.py --scale` множит холст и
  каждый прямоугольник, `render_actors.py --scale` — плотность рендера при той
  же камере.
- **Ассеты описываются дробными единицами мира.** Фаска в треть единицы, шов,
  блик на кромке появляются на крупном холсте и исчезают на мелком, где им
  всё равно не нашлось бы пикселя.
- **Текстуры перерисованы:** кладка с разнотоном, фаской и сколами; перекрытия
  с кромкой, желобком и выщербинами; двери с профилем рамы, филёнками, ручкой
  и порогом; лампа с патроном и кольцом; настил кабины с рифлением и заклёпками;
  ступень эскалатора с проступью и гребёнкой; створки шахты с утопленной
  филёнкой; выход с двустворчатой дверью и стрелкой на вывеске.
- **Пропорции переразмечены** (ADR-0018, решение 5): Otto и агенты выросли
  в полтора раза, дверь — со 102 до 171, окно уменьшилось до 150×100. Линия
  огня агента поднята до 105: подросшего Otto она начала доставать в голову
  прямо в проёме шахты.
- **Предел шахты виден** (решение 6): упор сверху и снизу полосы и стрелки
  в кабине, которые гаснут, когда в эту сторону ходу нет.
- **Тесты: 315** (было 304). Новые: пропорции (Otto проходит в дверь, входит
  в шахту, присев — ниже пули агента), управление каждой кабиной здания,
  предел полосы, указатели, упоры.

### Что нашлось по дороге

- **Кабина роняла Otto ниже этажа.** `settle_distance` остался в старом
  масштабе — 12 единиц при 36 за два кадра под `Engine.time_scale`. Кабина
  замирала на 24 единицы ниже пола, Otto оказывался внутри перекрытия и
  переставал двигаться. В ручном прогоне всё проходило, в тестах вставало
  намертво: GUT ждёт два кадра там, где написан один
  ([`testing.md`](testing.md), раздел 4).
- **Otto перестал помещаться в кабину,** когда вырос: просвет кабины был 102
  при росте 126. Кабина стала выше.
- **«Лифт не слушается» оказался концом полосы шахты,** а не багом: проверка
  всех кабин трёх зданий это показала. Лечится не механикой, а указателями.
- **Нижний упор шахты клался внутрь перекрытия** и не был виден вообще: знак,
  которым мы объясняли предел полосы, не работал ровно там, где игрок на него
  и наткнулся. Тест это пропускал — считал упоры и проверял X, но не Y. Нашло
  авторевью; тест усилен и проверен на старой формуле.
- **Оба генератора по умолчанию рисовали в масштабе 1,** хотя набор закоммичен
  в 3 (окружение) и 4.5 (актёры), и нигде в коде этих чисел не было. Команда из
  README молча переписала бы все ассеты втрое мельче. Теперь масштаб — значение
  по умолчанию, и регенерация без флагов даёт байт в байт закоммиченные PNG.

## Текущая веха — M14 · Дверь открывается по-настоящему

Решения — [ADR-0020](adr/0020-agent-doors.md).

**DoD:** ни один агент не появляется поверх закрытой двери, цикл двери
проверяется тестом без сцены, и цена телеграфа измерена ботом.

### Сделано

- **`DoorCycle` — ход створки отдельным классом без узла:** `CLOSED → OPENING →
  OPEN → CLOSING` и ход в долях. `DoorVisit` отдал ему створку и остался про
  правила визита. В 3D оба переезжают без правок (ADR-0019, решение 2).
- **Телеграф 0.7 с:** дверь открывается целиком и только потом отдаёт агента.
  Уровень просит дверь открыться, а сам агент появляется, когда створка дошла.
- **Открытая дверь занимает место под потолком живых.** Иначе за время
  телеграфа успело бы открыться сколько угодно дверей и агенты вывалились бы
  разом сверх восьми.
- **Агент выходит из проёма шагом** — `EMERGING` перестал быть «постоять»: он
  идёт, а раньше эти доли секунды стоял на коврике перед закрытой створкой.
- **В проёме агента нет на слое врагов:** пуля проходит сквозь, не гаснет и не
  приносит очков. Это не броня, а отсутствие цели — видно глазом.
- **Створка идёт обратно, как только агент освободил проём.**
- **Опустевшая красная дверь становится обычной** и начинает выпускать агентов.
- **Плавность — перекрёстное затухание трёх кадров.** Новых не рисуем: набор
  уходит вместе с 2D (ADR-0020, решение 7).
- **Дверь звучит позиционно,** источник заводится лениво: их полсотни на здание.
- **Тесты: 334** (было 315). Новые: ход створки без сцены (11), выход агента
  в собранном здании (8). Плюс `DoorVisit` переписан под пару с `DoorCycle`.

### Цена телеграфа: замер ботом

Решение 9 требовало измерить, а не решать на глаз. Шесть сидов, настоящее
здание, три жизни:

| | до | после |
|---|---|---|
| смертей | 8 | **5** |
| убийств | 95 | **65** |
| зданий провалено | 0 | 0 |

Опасность не обнулилась — бот по-прежнему умирает. Но вылезло **побочное:
убийств стало на треть меньше**, и это не про сложность, а про плотность.
Телеграф прибавляет 0.7 с к каждому циклу двери, и дверь всё это время занята:
агентов за то же время выходит меньше. Компенсация напрашивается — начинать
открывать створку в конце паузы поста, а не после неё, — но это правка баланса,
и её надо решать числами отдельно, а не заодно.

### Что нашлось по дороге

- **Два теста архитектуры молча опустели.** Они смотрят агентов через четыре
  кадра после сборки, а дверь теперь открывается 0.7 с — агентов в этот момент
  нет ни одного, и оба теста не делали ни одного утверждения. GUT пометил их
  Risky, и только поэтому это заметили. Теперь оба ждут первого вышедшего.
- **`greybox_level.gd` снова перевалил за 1000 строк.** Вынесена одежда шахт
  (`BuildingShafts`) — заодно закрыта находка авторевью M13: полсотни узлов
  висели прямыми детьми уровня, и под них попадал каждый обход детей.

### Авторевью

- **Otto запирался в двери навсегда.** Дверь, открывшаяся под агента, всё ещё
  пускала к себе гостя: `summon_agent` проверял «внутри никого», а `_admit` про
  агента не спрашивал. Уровень потом закрывал створку за вышедшим агентом, а
  отсидка гостя идёт только при открытой двери — и не кончалась никогда.
  Софтлок, воспроизводится тестом.
- **Дверь могла остаться открытой навсегда.** Створку закрывает уровень, и
  делал он это только в ветке живого агента. Убитый в кадре, где на одну
  отрисовку пришлось два шага физики, до этой ветки не доживает — дверь
  оставалась и открытой, и занятой: из неё больше не выходил никто. Теперь
  пустой пост отпускает дверь всегда.
- **Ближайшая дверь забирала кадр себе.** Выпускается один агент за кадр, у
  ближайшей двери; занятая отвечала отказом, и кадр пропадал впустую. Уровень
  спрашивает `Door.can_summon()` до выбора.
- **Проверка троса опустела.** `_parts` в тестах одежды перевели на узел шахт,
  а трос — прямой ребёнок уровня: «трос ушёл» проходило, ничего не найдя.
- **Щит агента ставится в `setup`,** а не первым кадром физики: иначе между
  постановкой в проём и первым кадром остаётся шаг, где он обычная мишень.
- **Мелочи:** картинка створки не перерисовывается, пока створка стоит
  (полсотни дверей на здание); `_agent_doors` убран — посты и так список
  агентских дверей; параметр `name` у `Door._say` заслонял поле `Node`.

## Куда идём: переезд в 3D

Решение — [ADR-0019](adr/0019-3d-pivot.md). Визуальной целью становится 3D-сцена
с ортокамерой сбоку вместо 2D-кадра из спрайтов. **Логика игры не меняется ни в
одном правиле:** ядро (`OttoStateMachine`, `ElevatorMotion`, `ShaftHazards`,
`BuildingPlan`, `BuildingRoute`, `EnemyBrain`, `DocumentRoute`) живёт на
`RefCounted` и `Resource`, без сцены и физики, и переезжает без правок. Меняются
слой узлов, пайплайн ассетов и свет.

Три правила на все вехи пивота:

1. **Каждая веха заканчивается играбельной сборкой.** Первая (M15) даёт коробки,
   а не красоту; пока 3D не проходит бота, `main` остаётся на 2D-сборке.
2. **Читаемость аркады важнее кинематографичности.** Игровой объект не зависит
   от освещения сцены: на пробе с погашенными лампами дверь исчезла полностью,
   а дверь — цель игры.
3. **Механика не живёт в узле.** Если правило оказалось в `_physics_process`,
   оно написано неправильно.

### Что показала проба

`tools/look3d.gd` — три этажа нашей же геометрии, ортокамера, PBR, SSR, SSAO,
объёмный туман, свечение. Кадры в `screens/look3d/`.

- **Движок не узкое место:** 1.8 мс при бюджете 16.6, запас девятикратный.
- **Ортокамера сбоку не видит пол.** Верхняя плоскость перекрытия — полоска
  нулевой толщины, и лампа, светящая вниз, освещает то, чего в кадре нет.
  Отражения в полу сбоку в чистом виде не работают; наклон камеры — открытый
  вопрос вехи света.
- **Свету нужна геометрия:** гладкий бокс читается размытым пятном, полкадра
  вытянули пилястры и торцы плит, а не источники.

## Принятые решения

| | |
|---|---|
| [ADR-0001](adr/0001-tech-stack.md) | Godot 4.7 и типизированный GDScript, без C# |
| [ADR-0002](adr/0002-visual-target.md) | HD пиксель-арт с динамическим светом; пайплайн уточнён в ADR-0011 |
| [ADR-0003](adr/0003-no-docker.md) | Docker не используется, кроме возможного M13 |
| [ADR-0004](adr/0004-elevator-mechanics.md) | Механика лифтов, шахт и эскалаторов по оригиналу |
| [ADR-0005](adr/0005-doors-and-documents.md) | Двери, документы и выход из здания |
| [ADR-0006](adr/0006-combat-and-enemies.md) | Бой, враги и разделение вехи M4 |
| [ADR-0007](adr/0007-lamps-and-darkness.md) | Лампы, темнота и неуязвимость на эскалаторе |
| [ADR-0008](adr/0008-building-generation.md) | Здание генерацией, разделение M5, тревога |
| [ADR-0009](adr/0009-game-loop-and-alarm.md) | Игровой цикл, тревога и рост сложности |
| [ADR-0010](adr/0010-lighting-and-atmosphere.md) | Свет, темнота, город за окнами |
| [ADR-0011](adr/0011-asset-pipeline.md) | Арт-пайплайн: Blender на актёров, код на окружение, разделение M7 |
| [ADR-0012](adr/0012-sound-and-interface.md) | Звук синтезом, свой мотив, шрифт с кириллицей, разделение M8 |
| [ADR-0013](adr/0013-release-and-versioning.md) | Релиз, версии и теги |
| [ADR-0014](adr/0014-building-architecture.md) | Крыша, силуэт здания и выпуск агентов |
| [ADR-0015](adr/0015-round-palette-and-roof.md) | Палитра раунда и крыша по Spectrum-версии |
| [ADR-0016](adr/0016-combat-balance.md) | Баланс боя и уклонение агентов |
| [ADR-0017](adr/0017-spectrum-palette-and-shafts.md) | Спектрумовская палитра, видимая шахта и крыша с тросом |
| [ADR-0018](adr/0018-native-fullhd.md) | Нативный FullHD, детализация ассетов, пропорции и предел шахты |
| [ADR-0019](adr/0019-3d-pivot.md) | Переезд на 3D-узлы; отменяет ADR-0002 и половину ADR-0010 и ADR-0011 |
| [ADR-0020](adr/0020-agent-doors.md) | Дверь агента: телеграф, проём и неуязвимость |

Прочее: вьюпорт 1920×1080 без скейла (ADR-0018 отменил 640×360 из ADR-0002,
ADR-0019 отменил и сам ADR-0002), приседание останавливает движение
(как в оригинале), прыжок из приседа запрещён. **Coyote time и буфер прыжка не добавляем:**
вопрос был отложен до конца M2, а формула проекта — механика 1983 года, и в оригинале
ни того, ни другого нет.

## Открытые вопросы

| # | Вопрос | Блокирует |
|---|---|---|
| 7 | Низкий выстрел агента: в оригинале он стреляет и с колена, у нас пуля всегда в 20 px над полом. Без низкого выстрела присевшего Otto не берёт вообще ничто | баланс боя |
| 6 | Баланс боя: замах 0.35 с, пороги уклонения 1.4 и 1.8, безопасное расстояние двери 96 px, потолок живых агентов 8 — подобраны замером, оригиналом не подтверждены | — |
| 3 | Целевая площадка: itch.io, Steam или только сборки в Releases | M9 |
| 4 | Сверка с оригиналом: остановка кабины между этажами, число красных дверей, правила респавна, длительность затемнения, момент тревоги | M2–M5 |

Вопрос 5 — согласованность кадров анимации при генерации — снят: кадры рендерятся из
одной модели в Blender, [ADR-0011](adr/0011-asset-pipeline.md), пункт 1.

## Долг и заметки

- **У приседа нет цены.** `shot_height` агента прибит константой −105 и от стойки
  не зависит, а присевший Otto ростом 81. Значит, присевшего не берёт вообще ничто:
  ни один агент, ни сколько угодно их разом. Ходить присев нельзя, и единственное,
  что наказывает за отсидку, — сирена. Открытый вопрос №7. **Решение принято
  2026-09-20 и припарковано вехой M16a** — отдельный тип агента в каске, стационарный
  стрелок с колена, доля растёт со злостью. Делать после пивота: приоритет у 3D.
- **Баланс боя не сверен.** Дальность агента 200 px, пауза между выстрелами 1.1 с,
  передышка после смерти 1.5 с, потолок живых агентов 8 — ни одно число оригиналом
  не подтверждено.
- **Спуска по тросу нет.** Крыша есть, Otto появляется на ней стоя. В оригинале он
  спускается на неё по тросу — это кадр вступления, не механика.
- **Внутренних стен, делящих этаж надвое, нет,** и шахты по-прежнему одной длины.
  Оригинал даёт и то и другое; обе вещи тянут за собой свой проход графа
  достижимости — ADR-0014, «Чего в вехе нет».
- **Действия `restart` и `quit_game` в `project.godot` осиротели:** R и Q вели
  отладочный оверлей, а его заменило меню — читателей у действий не осталось.
  Не удалены намеренно: переназначение клавиш будет разбираться со всей картой
  сразу, там и решится, остаются они или уходят.
- **Прогон тестов оставляет за собой утечку:** «6 ObjectDB instances were leaked»
  и «2 resources still in use» в конце. Проверено 2026-09-14: четыре экземпляра
  и оба ресурса даёт `test_building_playthrough.gd`, но дело не в тесте — то же
  самое печатает и обычный запуск игры на 300 кадров. Значит, что-то в собранном
  здании не освобождается при выходе. Проверки это не валит и в кадре не видно;
  разобрать, когда дойдут руки до сборки уровня.
- **Телеграф двери срезал плотность агентов на треть** (убийств 95 → 65 на
  шести сидах). Лечится тем, что створка начинает идти в конце паузы поста, а
  не после неё. Это правка баланса, и делать её надо отдельно, с замером.
- **Агенты не теряют след, когда Otto прячется за красной дверью.** Нашлось
  сверкой к M14: в оригинале войти в дверь значит сбить их с точной позиции,
  оставив лишь направление. У нас дверь на агентов не влияет вовсе.
- **Шум в картах нормалей усилен втрое.** `roughen()` в `render_env.py` сыплет
  шум попиксельно, а множитель `NORMAL_STRENGTH × scale` задуман для нарисованной
  геометрии: штукатурка и бетон под светом M6 шершавее задуманного. Правка на одну
  строку, но она инвалидирует ~150 закоммиченных карт нормалей. Сознательно не
  трогаем: весь этот набор уходит вместе с 2D (ADR-0019, решение 8).
- **Пять почти одинаковых ожиданий «пока Otto не встанет на ноги»** разъехались по
  `screenshotter.gd`, двум тестам и `round_shot.gd`, два из них байт в байт. Общий
  помощник напрашивается, но он трогает src, tests и tools разом — отдельной задачей.
- **Одежда шахты висит прямыми детьми уровня** — полсотни узлов на здание, ровно то,
  из-за чего `BuildingBackdrop` в своё время вынесли наружу. Каждый обход детей
  уровня теперь перебирает и их.
- GUT сообщает о доступной версии 9.7.1. Проверено 2026-09-12: на GitHub как latest
  по-прежнему помечена 9.6.1, она и вендорена. Обновить, когда 9.7.1 станет стабильной.
- **Темнота не слабеет в поздних зданиях**, хотя источники это описывают
  («in later levels, it doesn't work as well or at all»). Это правило сложности,
  и место ему рядом с `agent_menace` — ADR-0010, пункт 7.
- Новый `class_name` не виден движку, пока проект не переимпортирован: сцена,
  которая на него ссылается, молча возвращает `null`. Лечится
  `python tools/godot_check.py` — он импортирует проект.
- **Мануал Taito найден** — на Internet Archive, в текстовом виде. Надежды не
  оправдал: это руководство по обслуживанию автомата, и ни таблицы очков, ни числа
  красных дверей, ни правил тревоги в нём нет. Что он всё-таки дал: дополнительная
  жизнь за 10 000–25 000 очков, число жизней 3–6 (наши три — минимум из списка) и
  четыре уровня сложности для оператора зала. Остальное по-прежнему закрывается
  только MAME.
- **Переназначения клавиш нет.** Экран управления показывает раскладку, но не
  меняет её: настоящее переназначение — это ещё и геймпад, и разбор конфликтов,
  и сохранение схемы (ADR-0012, пункт 10). Отложено после релиза.
- **Демо-режима нет** (в автомате он крутится между партиями). Нужен бот, который
  играет сам; такой у нас есть только в тестах — ADR-0012, «Чего в вехе нет».

## Что было раньше

Итоги пройденных вех и находки авторевью — в [`milestones.md`](milestones.md).
Здесь только то, что описывает проект сегодня: иначе файл, который читают
первым в новой сессии, тонет в истории.
